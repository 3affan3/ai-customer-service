function getResponse(prompt){
    //UI should call this function with a prompt
    //Function should connect to 'getResponse' function in './LLM/main.py', and pass the 'prompt' function as parameter
    //Function should get the response back from python script, and then return it.
}